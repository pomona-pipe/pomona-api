version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 12
  pre_build:
    commands:
      - npm install -g typescript
      - npm install
  build:
    commands:
      - tsc && node ./dist
artifacts:
  files:
    - '**/*'